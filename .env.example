# Cloud sync configuration (optional)
# These are only needed if you want to enable cloud sync features.

# Google Drive API & Google Calendar API
# The same credentials work for both Drive backup and Calendar integration
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Google Drive API AND Google Calendar API
# 4. Create OAuth 2.0 credentials (Web application type)
# 5. Add your domain to authorized JavaScript origins
# 6. Create an API key and restrict it to Google Drive API and Google Calendar API
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_GOOGLE_API_KEY=your-google-api-key

# Dropbox API
# 1. Go to https://www.dropbox.com/developers/apps
# 2. Create a new app with "Scoped access" and "Full Dropbox" or "App folder"
# 3. Copy the App key
# 4. Add your redirect URI: https://yourdomain.com/dropbox-callback
VITE_DROPBOX_APP_KEY=your-dropbox-app-key
